{
  "name": "vidyouth-admin-api",
  "version": "0.1.0",
  "private": true,
  "description": "Vidyouth Admin Control Panel service (standalone, separate from the login API)",
  "type": "module",
  "main": "dist/server.js",
  "engines": {
    "node": ">=20.11.0"
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "type-check": "tsc --noEmit",
    "test": "node --import tsx --import ./tests/setup.ts --test --test-reporter=spec \"tests/**/*.test.ts\"",
    "test:unit": "node --import tsx --import ./tests/setup.ts --test --test-reporter=spec \"tests/permissions.test.ts\"",
    "test:integration": "node --import tsx --import ./tests/setup.ts --test --test-reporter=spec \"tests/admin.test.ts\""
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/helmet": "^11.1.1",
    "@fastify/rate-limit": "^9.1.0",
    "@fastify/sensible": "^5.6.0",
    "dotenv": "^16.4.5",
    "fastify": "^4.27.0",
    "ioredis": "^5.4.1",
    "jose": "^5.4.0",
    "pg": "^8.11.5",
    "pino-pretty": "^11.1.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "@types/pg": "^8.11.6",
    "tsx": "^4.11.2",
    "typescript": "^5.4.5"
  }
}
